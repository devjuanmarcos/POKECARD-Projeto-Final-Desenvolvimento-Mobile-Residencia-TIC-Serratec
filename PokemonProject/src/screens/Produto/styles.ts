import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        flex:1,
        paddingTop: 40,
        paddingHorizontal: 30
    },
    header:{
        marginStart: 65,
        paddingTop: 20,
        position:'relative',
        flexDirection:'row',
        alignItems: 'center',
        marginBottom: 15
    },
    title: {
        color: '#fff',
        fontSize: 24,
        fontWeight: 'bold',
        marginLeft: 20,
    },
    
})